
  # Build UI Components and Routes

  This is a code bundle for Build UI Components and Routes. The original project is available at https://www.figma.com/design/Wbd9BCi2AB9dTPP9T50xVt/Build-UI-Components-and-Routes.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  